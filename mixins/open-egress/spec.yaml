schemaVersion: "2"
kind: mixin
name: open-egress
displayName: Open egress (all domains)
description: >
  Adds an allow all network policy.
version: 0.1.0

permissions:
  network:
    allow:
      - "**"
