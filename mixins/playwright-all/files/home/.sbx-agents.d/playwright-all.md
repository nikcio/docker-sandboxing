## Playwright (all browsers)

- Playwright is installed globally (npm) — run it as `npx playwright ...`
- Preinstalled browsers: Chromium, Firefox, WebKit (headless use only —
  the sandbox has no display server)
