# syntax=docker/sandbox-kit:3
schemaVersion: "3"
kind: workload
displayName: OpenCode (Custom)
description: Shell workload (the shell base image + the sandbox's git
  setup) — compose the opencode mixin for the agent, plus the mixins your
  task needs.

capabilities:
  - type: com.docker.sandbox/sbx@1
  - type: com.docker.sandbox/agent-context@1
    config:
      filename: AGENTS.md
